# Базовый URL API. По умолчанию — реальный бэкенд (FastAPI, порт 8000).
# Для работы с Prism mock (npm run mock в корне, порт 4010) укажите:
# VITE_API_BASE_URL=http://localhost:4010
VITE_API_BASE_URL=http://localhost:8000
